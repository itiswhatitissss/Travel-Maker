<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.zerock.travelmaker.mapper.SampleMapper">

    <select id="getBoards" resultType="org.zerock.travelmaker.domain.SampleDomain">
        select * from board;
    </select>

<!--    <select id="findUno" resultType="org.zerock.travelmaker.domain.Users">-->
<!--        select uno from Users where id=#{id} and password=#{password}-->
<!--    </select>-->

</mapper>